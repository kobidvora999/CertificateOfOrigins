$kind: http-request
url: "{{baseUrl}}/AuthenticationRequest/HandleImportDeliveryAndReminderForVendorSent"
method: POST
headers:
  Content-Type: application/json
# Same endpoint with isDelivery=false - the REMINDER branch of AdvanceDeliveryStatus, which the delivery call above does not reach.
body:
  type: json
  content: |-
    {
      "id": 990001,
      "authenticationFileStatusId": 1,
      "deliveryMethodId": 1,
      "isDelivery": false
    }
order: 14000
