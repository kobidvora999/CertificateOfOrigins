$kind: http-request
url: "{{base_URL}}/AuthenticationRequest/EntityDocuments/{{leadDocumentId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      // Liveness: Documents answered — GetEntityDocuments fetches the entity's documents via Document/DocumentsByEntity.
      pm.test("Documents alive — HTTP 200", () => pm.expect(pm.response.code).to.equal(200));
    language: text/javascript
order: 1000
