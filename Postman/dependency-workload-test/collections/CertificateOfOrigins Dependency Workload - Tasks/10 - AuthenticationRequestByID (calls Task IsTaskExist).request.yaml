$kind: http-request
url: "{{base_URL}}/AuthenticationRequest/{{authenticationRequestDocumentId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      // Liveness: Tasks answered — GetAuthenticationRequestByID checks open tasks via Task/IsTaskExist.
      pm.test("Tasks alive — HTTP 200", () => pm.expect(pm.response.code).to.equal(200));
    language: text/javascript
order: 1000
