$kind: http-request
url: "{{base_URL}}/AuthenticationRequest/AuthenticationRequestByID/{{authenticationRequestDocumentId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      // Liveness: Collaterals answered — GetAuthenticationRequestByID resolves the request's collateral via Collateral/GetCollateralRequest.
      pm.test("Collaterals alive — HTTP 200", () => pm.expect(pm.response.code).to.equal(200));
    language: text/javascript
order: 1000
