$kind: http-request
url: "{{base_URL}}/AuthenticationRequest/AuthenticationRequestByID/{{authenticationRequestDocumentId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      // Liveness: Vendors answered — GetAuthenticationRequestByID enriches the vendor name via Vendor/VendorsByIds.
      pm.test("Vendors alive — HTTP 200", () => pm.expect(pm.response.code).to.equal(200));
    language: text/javascript
order: 1000
